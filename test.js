import { initializeApp } from 'firebase/app'; import { getFirestore, collection, addDoc, getDocs } from 'firebase/firestore'; const app = initializeApp({ projectId: 'ssr-team' }); const db = getFirestore(app); getDocs(collection(db, 'artifacts', 'ssr-team', 'public', 'data', 'players')).then(() =, e.message)); getDocs(collection(db, 'artifacts', 'ssr-team', 'public', 'data', 'teams')).then(() =, e.message));  
